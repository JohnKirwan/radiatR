# Circular coordinate utilities

A collection of helpers for converting between Cartesian coordinates and
unit-circle representations, including orientation conversions used
throughout the package. Most functions are internal, but the angle
wrapping helpers remain exported for backwards compatibility.
