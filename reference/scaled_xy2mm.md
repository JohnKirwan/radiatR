# Convert the given point from pixels to millimeters

Convert the given point from pixels to millimeters

## Usage

``` r
scaled_xy2mm(xy, f, F)
```

## Arguments

- xy:

  point (normalised coordinate after division by the focal lengths)

- f:

  focal length in pixels

- F:

  focal length in millimeters

## Value

point in millimeters
