# Write calibration points to CSV

Write calibration points to CSV

## Usage

``` r
write_calibration_points(image_points, path)
```

## Arguments

- image_points:

  Calibration point data (list or tibble).

- path:

  File path to write. Directories must already exist.

## Value

Invisibly returns \`path\`.
