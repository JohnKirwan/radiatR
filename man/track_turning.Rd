% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_metrics.R
\name{track_turning}
\alias{track_turning}
\title{Per-trajectory turning-rate summary for a Tracks}
\usage{
track_turning(
  ts,
  stat = c("mean_abs", "mean", "max_abs", "median_abs"),
  units = c("radians", "degrees"),
  x_col = ts@cols$x,
  y_col = ts@cols$y
)
}
\arguments{
\item{ts}{A `Tracks`.}

\item{stat}{`"mean_abs"` (default) the typical turning magnitude; `"mean"` the
signed net bias (opposite turns cancel); `"max_abs"` the sharpest turn;
`"median_abs"` a robust turning magnitude.}

\item{units}{`"radians"` per second (default) or `"degrees"` per second.}

\item{x_col, y_col}{Names of the coordinate columns. Default to the `Tracks`'s
recorded x/y columns.}
}
\value{
A `data.frame` with one row per trajectory: the id column and a numeric
  `turning` column (`NA` for tracks with fewer than three points).
}
\description{
Summarises each trajectory's [angular_velocity()] (signed turning rate,
counter-clockwise positive) by the chosen statistic.
}
\seealso{
[angular_velocity()], [track_velocity()], [track_speed()]
}
