% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular_plotting.R
\name{add_vonmises_density}
\alias{add_vonmises_density}
\title{Overlay a fitted von Mises density curve on a radiate plot}
\usage{
add_vonmises_density(
  fit,
  scale = 0.4,
  inner_r = 0,
  group_col = NULL,
  n_pts = 360L,
  colour = "steelblue",
  linewidth = 0.8,
  fill = NA,
  alpha = 0.8,
  display = NULL,
  axial = FALSE,
  color = NULL
)
}
\arguments{
\item{fit}{Data frame from \code{\link{vonmises_fit}}, containing at least
\code{mu} and \code{kappa} columns.}

\item{scale}{Maximum outer radius as a fraction of the unit circle.
Default \code{0.4} matches \code{add_angle_rose}.}

\item{inner_r}{Inner radius; default \code{0}.}

\item{group_col}{Column in \code{fit} for faceting; must match the
\code{panel_by} argument of the parent \code{radiate()} call.}

\item{n_pts}{Angular evaluation points.  Default \code{360L}.}

\item{colour, color}{Outline colour.  Default \code{"steelblue"}. \code{color}
is the American-spelling alias.}

\item{linewidth}{Outline width.  Default \code{0.8}.}

\item{fill}{Fill colour.  Default \code{NA} (outline only).}

\item{alpha}{Opacity.  Default \code{0.8}.}

\item{display}{A `circ_display()` controlling rotation, matching the parent
`radiate()` plot. Default `NULL` uses the input's `display` attribute when
present, otherwise the identity display.}

\item{axial}{Logical; when `TRUE`, draw the axial (bidirectional) density of an
axial fit (from `vonmises_fit(axial = TRUE)`): the curve is evaluated on
doubled angles, giving two equal peaks at `mu` and `mu + pi`. Default
`FALSE`.}
}
\value{
A \code{geom_polygon} layer, or \code{NULL} if \code{fit} is all
  \code{NA}.
}
\description{
Evaluates the von Mises probability density on a fine angular grid and
draws it as a closed polygon in the same Cartesian coordinate space used
by \code{\link{radiate}} and \code{\link{add_angle_rose}}.  The curve
peaks at \code{scale} so the two layers align when given matching
\code{scale} values.
}
