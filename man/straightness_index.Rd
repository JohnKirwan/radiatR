% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_metrics.R
\name{straightness_index}
\alias{straightness_index}
\title{Per-trajectory straightness index for a TrajSet}
\usage{
straightness_index(ts, x_col = ts@cols$x, y_col = ts@cols$y)
}
\arguments{
\item{ts}{A `TrajSet`.}

\item{x_col, y_col}{Names of the coordinate columns to use. Default to the
`TrajSet`'s recorded x/y columns (the real arena positions), so the metric
reflects the physical path rather than any display transform.}
}
\value{
A `data.frame` with one row per trajectory: the `TrajSet`'s id column
  and a numeric `straightness` column.
}
\description{
Computes [path_straightness()] for each trajectory in a `TrajSet`, ordering
each trajectory's points by its time column when one is recorded.
}
\seealso{
[path_straightness()]
}
