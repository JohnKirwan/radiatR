% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular_plotting.R
\name{degree_labs}
\alias{degree_labs}
\title{Label the four diagonal directions.}
\usage{
degree_labs(
  display = circ_display(),
  colour = "black",
  units = NULL,
  size = 3.88,
  family = "",
  position = c("outside", "inside", "split"),
  inside_radius = 0.88,
  color = NULL
)
}
\arguments{
\item{display}{A [`circ_display`] object. Default `circ_display()`. Supplies
the label units when `units` is `NULL`.}

\item{colour, color}{Label colour. Default `"black"`. `color` is the
American-spelling alias.}

\item{units}{`"degrees"` (e.g. `45°`) or `"radians"` (e.g. `π/4`). When
`NULL` (default) the units are taken from `display`.}

\item{size}{Label text size, in mm. Default `3.88` (ggplot2's default
text size).}

\item{family}{Label font family. Default `""` (the device default).}

\item{position}{Where to place the labels: `"outside"` (default) keeps the
four diagonals (45/135/225/315) in the plot corners, as before;
`"inside"` puts all eight directions (including the cardinals
0/90/180/270) just inside the circle at `inside_radius`; `"split"` puts
the cardinals inside and the diagonals outside. The `"inside"`/`"split"`
modes honour `display` (the labels rotate with the plot).}

\item{inside_radius}{Radius (unit-circle fraction) for inside labels.
Default `0.88`.}
}
\value{
A list of ggplot2 annotation layers.
}
\description{
Provides a list of annotation layers that mark 45, 135, 225, and 315 degrees on a
unit circle.
}
\examples{
library(ggplot2)
ggplot() +
  coord_fixed() +
  degree_labs()
}
