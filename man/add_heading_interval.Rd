% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular_plotting.R
\name{add_heading_interval}
\alias{add_heading_interval}
\title{Compute a circular interval arc and add it to a radial plot in one step}
\usage{
add_heading_interval(
  headings_df,
  heading_col = "heading",
  facets = NULL,
  group_col = NULL,
  display = NULL,
  stat = c("bootstrap_ci", "sd"),
  boot_reps = 1000L,
  boot_alpha = 0.05,
  radius = 1.05,
  linewidth = 1.5,
  colour = NULL,
  linetype = "solid",
  n_theta = 500L,
  axial = FALSE,
  color = NULL
)
}
\arguments{
\item{headings_df}{Data frame containing heading angles.}

\item{heading_col}{Name of the heading column (radians). Default `"heading"`.}

\item{facets}{Character vector of column names used for faceting (placement).
Passed to [compute_circ_interval()]; each column is attached to the layer
data so `facet_grid()`/`facet_wrap()` can place the arc correctly.
Default `NULL`.}

\item{group_col}{Single column name used for colour grouping. Passed to
[compute_circ_interval()] and then to [add_circ_interval()] as
`colour_col`. Default `NULL`.}

\item{display}{A [`circ_display`] object. When `NULL` (default), read from
`attr(headings_df, "display")`, falling back to `circ_display()`.}

\item{stat}{Statistic: `"bootstrap_ci"` (default) or `"sd"`.}

\item{boot_reps}{Integer. Bootstrap replicates for `stat = "bootstrap_ci"`.
Default `1000L`. Ignored when `stat = "sd"`.}

\item{boot_alpha}{Significance level for the bootstrap CI. Default `0.05`
produces a 95\% interval.}

\item{radius}{Radial position of the arc. Default `1.05`.}

\item{linewidth}{Line width. Default `1.5`.}

\item{colour, color}{Fixed colour. When `NULL` (default) and `colour_col` is
set, colour is mapped from that column; when `NULL` and no `colour_col`,
draws in `"black"`. Supplying any colour string always overrides
`colour_col`. `color` is the American-spelling alias.}

\item{linetype}{Line type. Default `"solid"`.}

\item{n_theta}{Number of points along the arc. Default `500L`.}

\item{axial}{Logical. Render the overlay for axial (bidirectional, mod-pi)
data via the angle-doubling method: the CI is drawn at both poles of the
axis. Default `FALSE`.}
}
\value{
A `geom_path()` layer.
}
\description{
Convenience wrapper that calls [compute_circ_interval()] followed by
[add_circ_interval()]. Use [compute_circ_interval()] + [add_circ_interval()]
directly when you need to replace `lower`/`upper` with Bayesian credible
interval bounds before rendering.
}
\seealso{
[compute_circ_interval()], [add_circ_interval()]
}
