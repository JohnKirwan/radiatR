% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Tracks.R
\docType{class}
\name{Tracks-class}
\alias{Tracks-class}
\alias{tracks}
\alias{length,Tracks-method}
\alias{[,Tracks,ANY,missing,missing-method}
\alias{c,Tracks-method}
\title{Tracks container for circular trajectories}
\usage{
tracks(
  df,
  id = "id",
  time = "time",
  angle = NULL,
  x = NULL,
  y = NULL,
  rel_x = NULL,
  rel_y = NULL,
  angle_unit = NULL,
  weight = NULL,
  normalize_xy = TRUE,
  meta = list(),
  transform_history = NULL
)

\S4method{length}{Tracks}(x)

\S4method{[}{Tracks,ANY,missing,missing}(x, i)

\S4method{c}{Tracks}(x, ..., recursive = FALSE)
}
\arguments{
\item{df}{data.frame in long form}

\item{id, time}{Columns naming trajectory id and time}

\item{angle}{Optional angle column (radians or degrees, see angle_unit)}

\item{x, y}{Optional cartesian columns; if provided, converted to unit circle and angle inferred}

\item{rel_x, rel_y}{Optional column names for pre-transformed relative
coordinates (centred on the origin, unit-circle scaled).  Both must be supplied
together or not at all.}

\item{angle_unit}{Units of provided angle ("radians" or "degrees"); stored internally as radians}

\item{weight}{Optional weight column name}

\item{normalize_xy}{If TRUE (default), (x,y) are scaled to the unit circle per trajectory:
each trajectory is centred on its bounding-box midpoint and scaled so its
furthest point sits at radius 1. This preserves trajectory shape and places
the origin at the centre (what the radius-based heading rules expect).
Raw coordinates are retained in `<x>_raw`/`<y>_raw`. If FALSE, (x,y) are kept
as supplied. (Landmark-based mapping, when available, is more accurate; this
is the no-landmark fallback.)}

\item{meta}{Free-form list of metadata}

\item{transform_history}{Optional tibble describing transformation steps applied to the
trajectories. Must contain columns `step`, `order`, `id`, `implementation`, `params`,
and `depends_on`.}

\item{i}{Trajectory identifiers (character ids, numeric indices, or logical vector)}

\item{...}{Additional Tracks objects to append}

\item{recursive}{Ignored; maintained for signature compatibility}
}
\value{
A Tracks S4 object
}
\description{
Tracks container for circular trajectories

Construct a Tracks
}
\section{Slots}{

\describe{
\item{\code{data}}{data.frame of trajectory observations in long form}

\item{\code{cols}}{list mapping required column names (id/time/angle/optional x,y,weight)}

\item{\code{angle_unit}}{character describing the original angle unit supplied}

\item{\code{meta}}{list of additional metadata attached to the set}
}}

