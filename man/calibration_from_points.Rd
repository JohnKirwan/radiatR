% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration_capture.R
\name{calibration_from_points}
\alias{calibration_from_points}
\title{Compute a calibration from saved calibration points}
\usage{
calibration_from_points(
  board_dims,
  square_size,
  image_points,
  origin = c(0, 0),
  order = c("row_major", "col_major"),
  quiet = FALSE
)
}
\arguments{
\item{board_dims}{Integer vector of length 2 giving the number of squares in
the checkerboard (rows, columns). Charuco and AprilTag grids use the same
layout assumption.}

\item{square_size}{Physical spacing between adjacent corners in world units.
A scalar applies to both axes; a length-two vector can be used for
anisotropic spacing.}

\item{image_points}{Calibration points supplied as a list, tibble, or file
path (as accepted by `load_points`).}

\item{origin}{World coordinates to assign to the (row = 0, col = 0) corner.}

\item{order}{Expected traversal order for the clicked points. `"row_major"`
(default) progresses left-to-right along each row before moving downward.}

\item{quiet}{Suppress informational output.}
}
\value{
Same structure as [calibration_session()] but without the interactive
  capture step.
}
\description{
Compute a calibration from saved calibration points
}
