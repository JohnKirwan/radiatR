% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coords.R
\name{distance_scale}
\alias{distance_scale}
\alias{distance_scale,Tracks-method}
\alias{distance_unit}
\alias{distance_unit,Tracks-method}
\alias{set_distance_scale}
\alias{set_distance_scale,Tracks-method}
\alias{calibrate_distance}
\title{Distance calibration for a Tracks object}
\usage{
distance_scale(x)

\S4method{distance_scale}{Tracks}(x)

distance_unit(x)

\S4method{distance_unit}{Tracks}(x)

set_distance_scale(x, scale, unit = NULL)

\S4method{set_distance_scale}{Tracks}(x, scale, unit = NULL)

calibrate_distance(ts, coord_distance, real_distance, unit = NULL)
}
\arguments{
\item{x}{A [Tracks] object.}

\item{scale}{A single positive number: physical units per coordinate unit.}

\item{unit}{Optional single string, the physical unit label (e.g. `"mm"`).}

\item{ts}{A [Tracks] object (for `calibrate_distance()`).}

\item{coord_distance, real_distance}{For `calibrate_distance()`, a known
separation in coordinate units and its real-world length; the scale is
`real_distance / coord_distance`.}
}
\value{
`distance_scale()`/`distance_unit()` the stored values (or `NULL`);
  `set_distance_scale()`/`calibrate_distance()` the modified `Tracks`.
}
\description{
Attach a physical-distance scale (and optional unit label) to a [Tracks] so
path lengths and speeds can be reported in real units. The scale is physical
units per unit of the recorded `x`/`y` coordinates; it is applied on demand
and the stored coordinates are never altered. radiatR otherwise analyses in
normalised (unit-circle) space -- this is an optional calibration hook.
}
\seealso{
[frame_rate()], [track_length()], [track_speed()]
}
