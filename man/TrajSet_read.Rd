% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loaders.R
\name{TrajSet_read}
\alias{TrajSet_read}
\title{Construct a TrajSet from a data.frame or file(s)}
\usage{
TrajSet_read(
  x,
  mapping = list(id = NULL, time = NULL, x = NULL, y = NULL, angle = NULL, weight = NULL),
  angle_unit = c("radians", "degrees", "auto"),
  time_type = c("auto", "posix", "seconds", "frames"),
  tz = "UTC",
  fps = NULL,
  normalize_xy = TRUE,
  dialect = NULL,
  mutate = NULL,
  keep = NULL,
  drop = NULL,
  id_from_filename = TRUE,
  validate = TRUE
)

TrajSet_read(
  x,
  mapping = list(id = NULL, time = NULL, x = NULL, y = NULL, angle = NULL, weight = NULL),
  angle_unit = c("radians", "degrees", "auto"),
  time_type = c("auto", "posix", "seconds", "frames"),
  tz = "UTC",
  fps = NULL,
  normalize_xy = TRUE,
  dialect = NULL,
  mutate = NULL,
  keep = NULL,
  drop = NULL,
  id_from_filename = TRUE,
  validate = TRUE
)
}
\arguments{
\item{x}{data.frame, file path, or character vector of file paths}

\item{mapping}{named list for column mapping: id, time, x, y, angle, weight. Any missing will be guessed when possible.}

\item{angle_unit}{"radians","degrees", or "auto" to guess from values}

\item{time_type}{one of "auto","posix","seconds","frames"}

\item{tz}{timezone for POSIX times (default "UTC")}

\item{fps}{frames-per-second when time_type = "frames"}

\item{normalize_xy}{TRUE to normalize (x,y) to unit circle when both provided}

\item{dialect}{optional registered dialect name to pre-process raw input}

\item{mutate}{list of transformations applied after reading (named functions or formulas)}

\item{keep}{only keep these columns (NULL = keep all)}

\item{drop}{drop these columns after mapping}

\item{id_from_filename}{if TRUE and id missing, derive id from file stem when reading multiple files}

\item{validate}{if TRUE run S4 validity checks}
}
\value{
TrajSet

TrajSet
}
\description{
Construct a TrajSet from a data.frame or file(s)

Construct a TrajSet from a data.frame or file(s)
}
