% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular_plotting.R
\name{add_heading_arrow}
\alias{add_heading_arrow}
\title{Compute a circular mean arrow and add it to a radial plot in one step}
\usage{
add_heading_arrow(
  headings_df,
  heading_col = "heading",
  colour_col = NULL,
  angle_convention = NULL,
  coords = NULL,
  linewidth = 1,
  colour = NULL,
  arrow_length_cm = 0.2,
  ...
)
}
\arguments{
\item{headings_df}{Data frame with a column of heading angles in radians.
[derive_headings()] sets `attr(headings_df, "angle_convention")` and
`attr(headings_df, "coords")` automatically.}

\item{heading_col}{Name of the column containing heading angles. Default
`"heading"`.}

\item{colour_col}{Optional. Name of a column to group by. One row is
returned per group. The same column maps to colour in [add_circ_mean()].}

\item{angle_convention}{Convention for angles in `heading_col`: `"clock"`
(0 = North, clockwise) or `"unit_circle"` (0 = East, CCW). If `NULL`
(default), read from `attr(headings_df, "angle_convention")`; defaults to
`"unit_circle"` with a message if the attribute is also absent.}

\item{coords}{Coordinate system: `"relative"` or `"absolute"`. If `NULL`
(default), read from `attr(headings_df, "coords")`; defaults to
`"absolute"` with a message if absent.}

\item{linewidth}{Line width of the arrow segment. Default `1`.}

\item{colour}{Fixed colour. When `NULL` (default) and `colour_col` is set,
colour is mapped from that column; when `NULL` and no `colour_col`, draws
in `"black"`. Supplying any colour string always overrides `colour_col`.}

\item{arrow_length_cm}{Arrowhead length in cm. Default `0.2`.}

\item{...}{Additional arguments forwarded to `geom_segment` (e.g.
`linetype`, `alpha`, or a custom `arrow` spec that overrides the default).}
}
\value{
A `geom_segment()` layer.
}
\description{
Convenience wrapper that calls [compute_circ_mean()] followed by
[add_circ_mean()]. Use the two-step form directly when you need to inspect
or modify the summary data frame before rendering.
}
\seealso{
[compute_circ_mean()], [add_circ_mean()]
}
