% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TrajSet.R
\name{transform_history}
\alias{transform_history}
\alias{transform_history,TrajSet-method}
\alias{log_transform}
\alias{log_transform,TrajSet-method}
\alias{set_transform_history}
\alias{set_transform_history,TrajSet-method}
\title{Transform history helpers for TrajSet objects}
\usage{
transform_history(x)

\S4method{transform_history}{TrajSet}(x)

log_transform(
  x,
  step,
  traj_ids = NULL,
  implementation = step,
  params = NULL,
  order = NULL,
  depends_on = NULL
)

\S4method{log_transform}{TrajSet}(
  x,
  step,
  traj_ids = NULL,
  implementation = step,
  params = NULL,
  order = NULL,
  depends_on = NULL
)

set_transform_history(x, history)

\S4method{set_transform_history}{TrajSet}(x, history)
}
\arguments{
\item{x}{A `TrajSet` object.}

\item{step}{Character identifier for the transform step.}

\item{traj_ids}{Character vector of trajectory identifiers affected by the step.
Defaults to all trajectories in `x` when `NULL`.}

\item{implementation}{Character label for the implementation used to apply
the step. Defaults to `step`.}

\item{params}{List-column of per-trajectory parameter sets (recycled when a
single entry is provided).}

\item{order}{Optional integer giving the execution order. When omitted the
step is appended to the end of the log.}

\item{depends_on}{Optional character vector naming prerequisite step(s).}

\item{history}{Tibble or list describing the full transform history to
replace.}
}
\value{
For `transform_history`, a tibble describing the recorded steps. For
  `log_transform` and `set_transform_history`, the updated `TrajSet` object.
}
\description{
Transform history helpers for TrajSet objects
}
