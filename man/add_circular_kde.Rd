% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular_plotting.R
\name{add_circular_kde}
\alias{add_circular_kde}
\title{Overlay a non-parametric circular kernel density estimate on a radiate plot}
\usage{
add_circular_kde(
  hd,
  angle_col = "heading",
  group_col = NULL,
  bw = NULL,
  scale = 0.4,
  inner_r = 0,
  n_pts = 512L,
  kernel = "vonmises",
  colour = "tomato",
  linewidth = 0.8,
  fill = NA,
  alpha = 0.8
)
}
\arguments{
\item{hd}{Data frame with a heading column in radians.}

\item{angle_col}{Column name.  Default \code{"heading"}.}

\item{group_col}{Column for faceting; must match \code{panel_by} in the
parent \code{radiate()} call.}

\item{bw}{Bandwidth (concentration).  \code{NULL} uses
\code{bw.nrd.circular} automatic selection.}

\item{scale}{Maximum outer radius as a fraction of the unit circle.
Default \code{0.4}.}

\item{inner_r}{Inner radius.  Default \code{0}.}

\item{n_pts}{Number of evaluation points.  Default \code{512L}.}

\item{kernel}{Kernel name passed to \code{density.circular}.  Default
\code{"vonmises"} (the kernel shape, not a model assumption).}

\item{colour}{Outline colour.  Default \code{"tomato"}.}

\item{linewidth}{Outline width.  Default \code{0.8}.}

\item{fill}{Fill colour.  Default \code{NA} (outline only).}

\item{alpha}{Opacity.  Default \code{0.8}.}
}
\value{
A \code{geom_polygon} layer, or \code{NULL} if estimation fails.
}
\description{
Estimates the circular density using \code{\link[circular]{density.circular}}
(no distributional assumptions) and draws it as a closed polygon in the same
Cartesian coordinate space used by \code{\link{radiate}} and
\code{\link{add_angle_rose}}.  Unlike \code{\link{add_vonmises_density}},
this makes no assumption about the shape of the distribution and handles
multimodal data naturally.
}
\details{
The \code{bw} parameter is a \emph{concentration} parameter (analogous to
\eqn{\kappa} of the von Mises kernel) — larger values produce a sharper,
data-following estimate; smaller values over-smooth towards uniform.
\code{NULL} (default) selects bandwidth automatically via
\code{\link[circular]{bw.nrd.circular}}.
}
