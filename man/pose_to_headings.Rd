% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/headings.R
\name{pose_to_headings}
\alias{pose_to_headings}
\title{Derive per-frame headings from pose data without a Tracks}
\usage{
pose_to_headings(
  df,
  anterior = NULL,
  posterior = NULL,
  theta_col = NULL,
  id_col = NULL,
  time_col = NULL,
  angle_convention = c("unit_circle", "clock")
)
}
\arguments{
\item{df}{Data frame with at least id, time, and keypoint or angle columns.}

\item{anterior}{Prefix of the anterior bodypart columns
(\code{<anterior>_x} and \code{<anterior>_y} must exist).}

\item{posterior}{Prefix of the posterior bodypart columns.}

\item{theta_col}{Name of a pre-computed orientation angle column (alternative
to \code{anterior}/\code{posterior}).}

\item{id_col}{Column identifying trials or individuals.  Auto-detected from
common names; defaults to a single group \code{"1"} if absent.}

\item{time_col}{Column of frame indices or timestamps.  Defaults to row
position if absent.}

\item{angle_convention}{\code{"unit_circle"} (default) or \code{"clock"}.}
}
\value{
Data frame with columns \code{id}, \code{time}, \code{heading} (in
  radians), with an \code{angle_convention} attribute for downstream
  compatibility.
}
\description{
Computes a heading angle for every row in \code{df} from either two
bodypart keypoint columns or a pre-computed orientation angle column.
Intended for tethered or mostly stationary subjects where the position
trajectory is absent or uninformative and body pose is the primary signal,
but also useful for extracting a dense heading time series from trajectory
data.  The output is compatible with \code{\link{circ_dispersion}},
\code{\link{sector_summary}}, \code{\link{add_heading_points}}, and
\code{\link{add_angle_rose}}.
}
