% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loaders.R
\name{load_manifest}
\alias{load_manifest}
\title{Load trajectories listed in a file table into a Tracks}
\usage{
load_manifest(
  file_tbl,
  track_dir,
  manifest = NULL,
  manifest_cols = NULL,
  mapping = NULL,
  angle_unit = c("radians", "degrees", "auto"),
  time_type = c("auto", "posix", "seconds", "frames"),
  tz = "UTC",
  fps = NULL,
  normalize_xy = FALSE,
  dialect = NULL,
  keep = NULL,
  drop = NULL,
  ...
)
}
\arguments{
\item{file_tbl}{Tibble returned by [import_tracks()], containing at least the
columns `basename` and `track`.}

\item{track_dir}{Directory containing the track files on disk.}

\item{manifest}{Optional data frame with a `file` column that matches
`file_tbl$basename`.}

\item{manifest_cols}{Optional named character vector (or list) mapping new
column names in `file_tbl` to column names present in `manifest`. When
`NULL`, all columns aside from `file` are carried over with the same names.}

\item{mapping}{Optional explicit column mapping passed to [read_tracks()].}

\item{angle_unit, time_type, tz, fps}{Passed to [read_tracks()].}

\item{normalize_xy}{Logical; normalise x/y to unit circle. Default
\code{FALSE}. See [tracks()] for why \code{TRUE} is shape-preserving
only, not calibration.}

\item{dialect}{Optional dialect name; passed to [read_tracks()].}

\item{keep, drop}{Column selection vectors passed to [read_tracks()].}

\item{...}{Additional arguments forwarded to [read_tracks()].}
}
\value{
A `Tracks` with metadata columns replicated for each observation.
}
\description{
This high-level helper combines the file discovery tibble returned by
[import_tracks()] with optional metadata from an experiment manifest, reads
each track file using [read_tracks()], and merges the results into a single
`Tracks`.
}
