% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular_plotting.R
\name{add_angle_rose}
\alias{add_angle_rose}
\title{Add a rose diagram of heading angles to a radiate plot}
\usage{
add_angle_rose(
  hd,
  bins = 12L,
  angle_col = "heading",
  group_col = NULL,
  scale = 0.4,
  inner_r = 0,
  normalize = TRUE,
  fill = "steelblue",
  colour = NA,
  alpha = 0.5,
  arc_pts = 20L,
  display = NULL
)
}
\arguments{
\item{hd}{Data frame of headings, e.g. from \code{\link{pose_to_headings}}
or \code{derive_headings(\ldots, frame_select = "all")}.}

\item{bins}{Integer; number of equal angular sectors.  Default \code{12}.}

\item{angle_col}{Column containing headings in radians.  Default
\code{"heading"}.}

\item{group_col}{Column used for faceting; must match the \code{panel_by}
argument of the parent \code{radiate()} call.}

\item{scale}{Maximum outer radius of the tallest wedge as a fraction of the
unit circle radius.  Default \code{0.4}.}

\item{inner_r}{Inner radius of wedges.  Default \code{0}; set \code{> 0}
for a donut style.}

\item{normalize}{\code{TRUE} (default) scales wedges by proportion;
\code{FALSE} uses raw counts.}

\item{fill}{Wedge fill colour.  Default \code{"steelblue"}.}

\item{colour}{Wedge border colour.  Default \code{NA} (no border).}

\item{alpha}{Opacity.  Default \code{0.5}.}

\item{arc_pts}{Points used to approximate each wedge arc.  Default \code{20L}.}

\item{display}{A `circ_display()` controlling rotation, matching the parent
`radiate()` plot. Default `NULL` uses the input's `display` attribute when
present, otherwise the identity display.}
}
\value{
A \code{geom_polygon} layer that can be added to a \code{radiate()}
  plot with \code{+}.
}
\description{
Draws angular frequency as filled wedge polygons in the Cartesian coordinate
space used by \code{\link{radiate}}.  Each wedge spans one angular bin; its
outer radius is proportional to the proportion (or count) of frames in that
bin.  The layer can be faceted by passing the same column used in the parent
\code{radiate(panel_by = ...)} call.
}
