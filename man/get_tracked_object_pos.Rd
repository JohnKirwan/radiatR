% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular_trials.R
\name{get_tracked_object_pos}
\alias{get_tracked_object_pos}
\title{Derive trial-level track positions in polar coordinates.}
\usage{
get_tracked_object_pos(
  trial_limits,
  animal_track,
  circ0 = 0.1,
  circ1 = 0.2,
  radius_criterion = c("first_past", "closest")
)
}
\arguments{
\item{trial_limits}{Data frame produced by [get_trial_limits()].}

\item{animal_track}{Data frame or `TrajSet` of Cartesian coordinates for the
entire video.}

\item{circ0}{Inner radius threshold (default `0.1`).}

\item{circ1}{Outer radius threshold (default `0.2`).}

\item{radius_criterion}{Strategy for choosing the radius landmarks. `"first_past"`
selects the first point beyond each threshold, while `"closest"` chooses the
closest sample to the specified radius.}
}
\value{
A `TrajSet` containing all valid trial observations. The corresponding
  trial limits (including `valid_track` flags) are stored in
  `meta$trial_limits`.
}
\description{
Using the trial limits returned by [get_trial_limits()], this helper extracts
the corresponding rows from a track data frame or `TrajSet`, centres and
scales the coordinates, and computes angles in both absolute and
stimulus-relative frames. The function optionally controls how inner/outer
radius crossings are selected.
}
