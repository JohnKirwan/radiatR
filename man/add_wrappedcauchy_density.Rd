% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular_plotting.R
\name{add_wrappedcauchy_density}
\alias{add_wrappedcauchy_density}
\title{Overlay a fitted wrapped Cauchy density curve on a radiate plot}
\usage{
add_wrappedcauchy_density(
  fit,
  scale = 0.4,
  inner_r = 0,
  group_col = NULL,
  n_pts = 360L,
  colour = "darkorange",
  linewidth = 0.8,
  fill = NA,
  alpha = 0.8,
  display = NULL
)
}
\arguments{
\item{fit}{Data frame from \code{\link{wrappedcauchy_fit}}, containing at
least \code{mu} and \code{rho} columns.}

\item{scale}{Maximum outer radius as a fraction of the unit circle.
Default \code{0.4}.}

\item{inner_r}{Inner radius.  Default \code{0}.}

\item{group_col}{Column for faceting; must match \code{panel_by} in the
parent \code{radiate()} call.}

\item{n_pts}{Angular evaluation points.  Default \code{360L}.}

\item{colour}{Outline colour.  Default \code{"darkorange"}.}

\item{linewidth}{Outline width.  Default \code{0.8}.}

\item{fill}{Fill colour.  Default \code{NA} (outline only).}

\item{alpha}{Opacity.  Default \code{0.8}.}

\item{display}{A `circ_display()` controlling rotation, matching the parent
`radiate()` plot. Default `NULL` uses the input's `display` attribute when
present, otherwise the identity display.}
}
\value{
A \code{geom_polygon} layer, or \code{NULL} if estimation fails.
}
\description{
Evaluates \code{\link[circular]{dwrappedcauchy}} on a fine angular grid and
draws it as a closed polygon in the same Cartesian space as
\code{\link{radiate}}.  Intended as a visual companion to
\code{\link{add_vonmises_density}}: overlaying both curves shows whether the
data favour the lighter-tailed von Mises or the heavier-tailed wrapped
Cauchy.
}
\details{
Default colour is \code{"darkorange"} to distinguish from
\code{add_vonmises_density} (\code{"steelblue"}) and
\code{add_circular_kde} (\code{"tomato"}).
}
\seealso{
\code{\link{add_vonmises_density}}, \code{\link{add_circular_kde}}
}
